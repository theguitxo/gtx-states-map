import { InfoBoxDirective } from './info-box.directive';

describe('InfoBoxDirective', () => {
  it('should create an instance', () => {
    const directive = new InfoBoxDirective();
    expect(directive).toBeTruthy();
  });
});
